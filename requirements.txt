# Core
pandas>=1.3.0
python-dotenv>=0.19.0
requests>=2.26.0

# Database
sqlalchemy>=1.4.0
psycopg2-binary>=2.9.0

# Dashboard
streamlit>=1.10.0
watchdog>=2.1.0

# Binance SDK (Internal dependency, assumed to be installed or vendored)
# If using a public SDK, replace this. Since you have a custom 'binance_sdk_derivatives_trading_usds_futures' folder,
# you might need to copy that folder into this project or install it as a package.
binance-sdk-derivatives-trading-usds-futures